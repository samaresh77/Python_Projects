# A virtual environment gives each project its own isolated packages:
# Why do we need a virtual environment?

# Creating a Virtual Environment
# python3 -m venv .venv

# Activating the Environment on Mac
# source .venv/bin/activate